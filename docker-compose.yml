version: "3.7"

services:
  postgres:
    container_name: postgres
    image: postgres
    env_file:
      - .env
    expose:
      - 5432
    volumes:
      - ./services/db/data:/var/lib/postgresql/data/
    build:
      context: ./services/db/
      dockerfile: Dockerfile
